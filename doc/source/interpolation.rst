Interpolation
=============

There are many available methods to interpolate points when solving global models. *dolo* provides several of them.
They can been used with exactly the same interface. By default, *dolo* uses smolyak interpolation.

smolyak
-------

.. automodule:: dolo.numeric.interpolation.smolyak
   :members:


linear (delaunay tessellation)
------------------------------

multilinear
-----------

splines
-------
