name: Bewley
model_type: aggregation

free_parameter: [r]

population:
    agent:
        index:
            i: 100
        problem: bewley_dtcscc

distribution:
    beta:   i -> 0.945 + 0.0005*i

equilibrium:
    - sum([i], m[i]-c[i]) = 0 # total savings is 0
